<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="stylesheet" type="text/css" href="{{ asset('css/screen.css') }}" media="screen,projection" />
        <link rel="stylesheet" type="text/css" href="{{ asset('css/print.css') }}" media="print" />
        <link rel="stylesheet" type="text/css" href="{{ asset('css/main.css') }}" media="screen" />
        <!--[if lt IE 8]>
            <link rel="stylesheet" href="{{ asset('css/ie.css') }}" type="text/css" media="screen, projection">
        <![endif]-->
    </head>
    <body>

    <div class="container prepend-top">
        <div id="header" class="clearfix">
            <div id="login-bar" class="span-18">
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <strong>{{ app.user.username }}</strong> |
                <a href="{{ path('fos_user_security_logout') }}">
                    {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                </a>
                {% else %}
                <a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a> |
                <a href="{{ path('fos_user_registration_register') }}">{{ 'layout.register'|trans({}, 'FOSUserBundle') }}</a>
                {% endif %}
            </div>
            <h1 id="logo" class="span-6 last">
                <div class="span-3">@acid</div>
                <a href="{{ path('_welcome') }}" class="span-3 last">
                    <img src="{{ asset('images/hyperreal.png') }}" alt="logo" />
                </a>
            </h1>
        </div>

        <aside class="span-4">
            {% if is_granted('ROLE_ADMIN') %}
            <menu class="admin">
                <ul>
                    <li>
                        <a href="{{ path('admin_advertiser') }}">{{ 'advertisers'|trans({}, 'HyperAdsBundle') }}</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_page') }}">{{ 'pages'|trans({}, 'HyperAdsBundle') }}</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_zone') }}">{{ 'zones'|trans({}, 'HyperAdsBundle') }}</a>
                    </li>

                    <li>
                        <a href="{{ path('admin_banner') }}">{{ 'banners'|trans({}, 'HyperAdsBundle') }}</a>
                    </li>

                </ul>
            </menu>
            {% endif %}
            {% if is_granted("ROLE_USER") %}
            <menu class="user">
                <ul>
                    <li>
                        <a href="{{ path('user_banner_list') }}">{{ 'your.banners'|trans({}, 'HyperAdsBundle') }}</a>
                    </li>
                </ul>
            </menu>
            {% endif %}
        </aside>

        <div id="content" {% if is_granted("ROLE_USER") %}class="span-20 last"{% endif %}>
            {% if app.session.hasFlash('error') %}
            <div class="error">
                {{ app.session.flash('error') }}
            </div>
            {% endif %}
            {% if app.session.hasFlash('notice') %}
            <div class="notice">
                {{ app.session.flash('notice') }}
            </div>
            {% endif %}
            {% if app.session.hasFlash('info') %}
            <div class="info">
                {{ app.session.flash('info') }}
            </div>
            {% endif %}
            {% block body %}{% endblock %}
        </div>

        <div id="footer" class="span-24 prepend-top">
            {{ 'powered.by.acid'|trans({}, 'HyperAdsBundle') }} | <a href="https://github.com/hyperreal/AcidAdServer" class="github-fork">fork on github</a>
        </div>

    </div>

        {% block javascripts %}{% endblock %}
    </body>
</html>
