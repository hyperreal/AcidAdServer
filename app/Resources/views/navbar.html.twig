<nav class="navbar navbar-fixed-top" role="navigation">

    <div class="navbar-header">
        <a class="navbar-brand" href="{{ url('_welcome') }}">{{ 'layout.title'|babel }}</a>
    </div>

    <div class="container">
        <ul class="nav navbar-nav">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <li>
                    <a href="{{ url('fos_user_profile_edit') }}">{{ 'edit.profile'|babel|lower }}</a>
                </li>
                <li>
                    <a href="{{ path('fos_user_security_logout') }}">
                        {{ 'layout.logout'|trans({}, 'FOSUserBundle')|lower }}
                    </a>
                </li>
            {% else %}
                <li>
                    <a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                </li>
                <li>
                    <a href="{{ path('fos_user_registration_register') }}">{{ 'layout.register'|trans({}, 'FOSUserBundle') }}</a>
                </li>
            {% endif %}
        </ul>
        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            <p class="pull-right navbar-text">{{ 'layout.navbar.login-info'|babel({'%user%': app.user.username})|raw }}</p>
        {% endif %}
    </div>
</nav>