{% extends "::base.html.twig" %}
{% block title %}
{{ 'list.advertiser'|babylon }}
{% endblock %}
{% block body %}
<h1>{{ 'list.advertiser'|babylon }}</h1>

{% if entities|length > 0 %}
<table class="records_list">
    <thead>
    <tr>
        <th>{{ 'id'|babylon }}</th>
        <th>{{ 'name'|babylon }}</th>
        <th>{{ 'email'|babylon }}</th>
        <th>{{ 'actions'|babylon }}</th>
    </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
    <tr>
        <td><a href="{{ path('admin_advertiser_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
        <td>{{ entity.username }}</td>
        <td>{{ entity.email }}</td>
        <td>
            <ul>
                <li>
                    <a href="{{ path('admin_advertiser_show', { 'id': entity.id }) }}">
                        {{ 'show'|babylon }}
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_advertiser_edit', { 'id': entity.id }) }}">
                        {{ 'edit'|babylon }}
                    </a>
                </li>
            </ul>
        </td>
    </tr>
    {% endfor %}
    </tbody>
</table>
{% else %}
<div class="message">{{ 'no.entities'|babylon }}</div>
{% endif %}

<ul>
    <li>
        <a href="{{ path('admin_advertiser_new') }}">
            {{ 'create.new.entry'|babylon }}
        </a>
    </li>
</ul>

{% endblock %}