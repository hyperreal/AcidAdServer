{% extends "::base.html.twig" %}
{% block body %}
{% block title %}
{{ 'show.advertiser'|babylon({'advertiserName': entity.username}) }}
{% endblock %}
<h1>{{ 'show.advertiser'|babylon({'advertiserName': entity.username}) }}</h1>

<table class="record_properties">
    <tbody>
    <tr>
        <th>{{ 'id'|babylon }}</th>
        <td>{{ entity.id }}</td>
    </tr>
    <tr>
        <th>{{ 'name'|babylon }}</th>
        <td>{{ entity.username }}</td>
    </tr>
    <tr>
        <th>{{ 'email'|babylon }}</th>
        <td>{{ entity.email }}</td>
    </tr>
    <tr>
        <th>{{ 'firstname'|babylon }}</th>
        <td>{{ entity.firstName }}</td>
    </tr>
    <tr>
        <th>{{ 'lastname'|babylon }}</th>
        <td>{{ entity.lastName }}</td>
    </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_advertiser') }}">
            {{ 'back.to.the.list'|babylon }}
        </a>
    </li>
    <li>
        <a href="{{ path('admin_advertiser_edit', { 'id': entity.id }) }}">
            {{ 'edit'|babylon }}
        </a>
    </li>
    <li>
        <form action="{{ path('admin_advertiser_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">{{ 'delete'|babylon }}</button>
        </form>
    </li>
</ul>
{% endblock %}