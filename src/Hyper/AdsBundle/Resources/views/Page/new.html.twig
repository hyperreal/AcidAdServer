{% extends "::base.html.twig" %}
{% block title %}
{{ 'new.page'|babel }}
{% endblock %}
{% block body %}
<h1>{{ 'new.page'|babel }}</h1>

<form action="{{ path('admin_page_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <div class="form-actions">
        <a href="{{ path('admin_page') }}" class="btn">{{ 'back.to.the.list'|babel }}</a>
        <div class="pull-right">
            <button type="submit" class="btn btn-primary">{{ 'create'|babel }}</button>
        </div>
    </div>
</form>
{% endblock %}