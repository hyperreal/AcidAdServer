{% extends "::base.html.twig" %}
{% block title %}
{{ 'show.page'|trans({'pageName': entity.name}, 'HyperAdsBundle') }}
{% endblock %}
{% block body %}
<h1>{{ 'show.page'|trans({'pageName': entity.name}, 'HyperAdsBundle') }}</h1>

<table class="record_properties">
    <tbody>
    <tr>
        <th>{{ 'id'|trans({}, 'HyperAdsBundle') }}</th>
        <td>{{ entity.id }}</td>
    </tr>
    <tr>
        <th>{{ 'name'|trans({}, 'HyperAdsBundle') }}</th>
        <td>{{ entity.name }}</td>
    </tr>
    <tr>
        <th>{{ 'url'|trans({}, 'HyperAdsBundle') }}</th>
        <td>{{ entity.url }}</td>
    </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_page') }}">
            {{ 'back.to.the.list'|trans({}, 'HyperAdsBundle') }}
        </a>
    </li>
    <li>
        <a href="{{ path('admin_page_edit', { 'id': entity.id }) }}">
            {{ 'edit'|trans({}, 'HyperAdsBundle') }}
        </a>
    </li>
    <li>
        <form action="{{ path('admin_page_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">{{ 'delete'|trans({}, 'HyperAdsBundle') }}</button>
        </form>
    </li>
</ul>
{% endblock %}