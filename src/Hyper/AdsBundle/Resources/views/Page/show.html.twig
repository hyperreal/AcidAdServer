{% extends "::base.html.twig" %}
{% block title %}
{{ 'show.page'|babylon({'pageName': entity.name}) }}
{% endblock %}
{% block body %}
<h1>{{ 'show.page'|babylon({'pageName': entity.name}) }}</h1>

<table class="record_properties">
    <tbody>
    <tr>
        <th>{{ 'id'|babylon }}</th>
        <td>{{ entity.id }}</td>
    </tr>
    <tr>
        <th>{{ 'name'|babylon }}</th>
        <td>{{ entity.name }}</td>
    </tr>
    <tr>
        <th>{{ 'url'|babylon }}</th>
        <td>{{ entity.url }}</td>
    </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_page') }}">
            {{ 'back.to.the.list'|babylon }}
        </a>
    </li>
    <li>
        <a href="{{ path('admin_page_edit', { 'id': entity.id }) }}">
            {{ 'edit'|babylon }}
        </a>
    </li>
    <li>
        <form action="{{ path('admin_page_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">{{ 'delete'|babylon }}</button>
        </form>
    </li>
</ul>
{% endblock %}