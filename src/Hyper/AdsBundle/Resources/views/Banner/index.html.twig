{% extends "::base.html.twig" %}
{% block title %}
{{ 'list.banner'|babylon }}
{% endblock %}
{% block body %}
<h1>{{ 'list.banner'|babylon }}</h1>

{% if entities|length > 0 %}
<table class="records_list">
    <thead>
    <tr>
        <th>{{ 'id'|babylon }}</th>
        <th>{{ 'file'|babylon }}</th>
        <th>{{ 'extension'|babylon }}</th>
        <th>{{ 'width'|babylon }}</th>
        <th>{{ 'height'|babylon }}</th>
        <th>{{ 'type'|babylon }}</th>
        <th>{{ 'actions'|babylon }}</th>
    </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
    <tr>
        <td><a href="{{ path('admin_banner_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
        <td>{{ entity.file }}</td>
        <td>{{ entity.extension }}</td>
        <td>{{ entity.width }}</td>
        <td>{{ entity.height }}</td>
        <td>{{ entity.type }}</td>
        <td>
            <ul>
                <li>
                    <a href="{{ path('admin_banner_show', { 'id': entity.id }) }}">
                        {{ 'show'|babylon }}
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_banner_edit', { 'id': entity.id }) }}">
                        {{ 'edit'|babylon }}
                    </a>
                </li>
            </ul>
        </td>
    </tr>
    {% endfor %}
    </tbody>
</table>
{% else %}
<div class="message">{{ 'no.entities'|babylon }}</div>
{% endif %}
<ul>
    <li>
        <a href="{{ path('admin_banner_new') }}">
            {{ 'create.new.entry'|babylon }}
        </a>
    </li>
</ul>
{% endblock %}