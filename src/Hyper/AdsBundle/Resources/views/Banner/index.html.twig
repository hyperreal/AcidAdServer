{% extends "::base.html.twig" %}
{% block title %}
{{ 'list.banner'|babel }}
{% endblock %}
{% block body %}
<h1>{{ 'list.banner'|babel }}</h1>

{% if entities|length > 0 %}
<table class="records_list">
    <thead>
    <tr>
        <th>{{ 'id'|babel }}</th>
        <th>{{ 'file'|babel }}</th>
        <th>{{ 'extension'|babel }}</th>
        <th>{{ 'width'|babel }}</th>
        <th>{{ 'height'|babel }}</th>
        <th>{{ 'type'|babel }}</th>
        <th>{{ 'actions'|babel }}</th>
    </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
    <tr>
        <td><a href="{{ path('admin_banner_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
        <td>{{ entity.file }}</td>
        <td>{{ entity.extension }}</td>
        <td>{{ entity.width }}</td>
        <td>{{ entity.height }}</td>
        <td>{{ entity.type }}</td>
        <td>
            <ul>
                <li>
                    <a href="{{ path('admin_banner_show', { 'id': entity.id }) }}">
                        {{ 'show'|babel }}
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_banner_edit', { 'id': entity.id }) }}">
                        {{ 'edit'|babel }}
                    </a>
                </li>
            </ul>
        </td>
    </tr>
    {% endfor %}
    </tbody>
</table>
{% else %}
<div class="message">{{ 'no.entities'|babel }}</div>
{% endif %}
<ul>
    <li>
        <a href="{{ path('admin_banner_new') }}">
            {{ 'create.new.entry'|babel }}
        </a>
    </li>
</ul>
{% endblock %}