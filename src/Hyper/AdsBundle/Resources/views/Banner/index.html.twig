{% extends "::base.html.twig" %}
{% block title %}
{{ 'list.banner'|trans({}, 'HyperAdsBundle') }}
{% endblock %}
{% block body %}
<h1>{{ 'list.banner'|trans({}, 'HyperAdsBundle') }}</h1>

{% if entities|length > 0 %}
<table class="records_list">
    <thead>
        <tr>
            <th>{{ 'id'|trans({}, 'HyperAdsBundle') }}</th>
            <th>File</th>
            <th>Extension</th>
            <th>Width</th>
            <th>Height</th>
            <th>Type</th>
            <th>{{ 'actions'|trans({}, 'HyperAdsBundle') }}</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_banner_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.file }}</td>
            <td>{{ entity.extension }}</td>
            <td>{{ entity.width }}</td>
            <td>{{ entity.height }}</td>
            <td>{{ entity.type }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_banner_show', { 'id': entity.id }) }}">
                            {{ '__show'|trans({}, 'HyperAdsBundle') }}
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('admin_banner_edit', { 'id': entity.id }) }}">
                            {{ '__edit'|trans({}, 'HyperAdsBundle') }}
                        </a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% else %}
<div class="message">{{ 'no.entities'|trans({}, 'HyperAdsBundle') }}</div>
{% endif %}
<ul>
    <li>
        <a href="{{ path('admin_banner_new') }}">
            {{ 'create.new.entry'|trans({}, 'HyperAdsBundle') }}
        </a>
    </li>
</ul>
{% endblock %}