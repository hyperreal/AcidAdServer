{% extends "::base.html.twig" %}
{% block title %}
{{ 'new.banner'|babylon }}
{% endblock %}
{% block body %}
<h1>{{ 'new.banner'|babylon }}</h1>

<form action="{{ path('admin_banner_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">{{ 'create'|babylon }}</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_banner') }}">
            {{ 'back.to.the.list'|babylon }}
        </a>
    </li>
</ul>
{% endblock %}