{% extends "::base.html.twig" %}
{% block title %}
{{ 'show.banner'|babylon }}
{% endblock %}
{% block body %}
<h1>Banner</h1>

<table class="record_properties">
    <tbody>
    <tr>
        <th>{{ 'id'|babylon }}</th>
        <td>{{ entity.id }}</td>
    </tr>
    <tr>
        <th>{{ 'file'|babylon }}</th>
        <td>{{ entity.path }}</td>
    </tr>
    <tr>
        <th>{{ 'extension'|babylon }}</th>
        <td>{{ entity.extension }}</td>
    </tr>
    <tr>
        <th>{{ 'width'|babylon }}</th>
        <td>{{ entity.width }}</td>
    </tr>
    <tr>
        <th>{{ 'height'|babylon }}</th>
        <td>{{ entity.height }}</td>
    </tr>
    <tr>
        <th>{{ 'type'|babylon }}</th>
        <td>{{ entity.type }}</td>
    </tr>
    <tr>
        <th>{{ 'title'|babylon }}</th>
        <td>{{ entity.title }}</td>
    </tr>
    <tr>
        <th>{{ 'linktitle'|babylon }}</th>
        <td>{{ entity.linkTitle }}</td>
    </tr>
    <tr>
        <th>{{ 'url'|babylon }}</th>
        <td>{{ entity.url }}</td>
    </tr>
    <tr>
        <th>{{ 'description'|babylon }}</th>
        <td>{{ entity.description }}</td>
    </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_banner') }}">
            {{ 'back.to.the.list'|babylon }}
        </a>
    </li>
    <li>
        <a href="{{ path('admin_banner_edit', { 'id': entity.id }) }}">
            {{ 'edit'|babylon }}
        </a>
    </li>
    <li>
        <form action="{{ path('admin_banner_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">{{ 'delete'|babylon }}</button>
        </form>
    </li>
</ul>
{% endblock %}