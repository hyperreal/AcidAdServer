{% extends "::base.html.twig" %}
{% block title %}
{{ 'list.zone'|trans({}, 'HyperAdsBundle') }}
{% endblock %}
{% block body %}
<h1>Zone list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Enabled</th>
            <th>Maxwidth</th>
            <th>Maxheight</th>
            <th>Type</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_zone_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.enabled }}</td>
            <td>{{ entity.maxWidth }}</td>
            <td>{{ entity.maxHeight }}</td>
            <td>{{ entity.type }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_zone_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_zone_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('admin_zone_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}