{% extends "::base.html.twig" %}
{% block title %}
{{ 'show.zone'|babylon({'zoneName': entity.name}) }}
{% endblock %}
{% block body %}
<h1>{{ 'show.zone'|babylon({'zoneName': entity.name}) }}</h1>

<table class="record_properties">
    <tbody>
    <tr>
        <th>{{ 'id'|babylon }}</th>
        <td>{{ entity.id }}</td>
    </tr>
    <tr>
        <th>{{ 'name'|babylon }}</th>
        <td>{{ entity.name }}</td>
    </tr>
    <tr>
        <th>{{ 'name'|babylon }}</th>
        <td>{{ entity.enabled }}</td>
    </tr>
    <tr>
        <th>{{ 'max.width'|babylon }}</th>
        <td>{{ entity.maxWidth }}</td>
    </tr>
    <tr>
        <th>{{ 'max.height'|babylon }}</th>
        <td>{{ entity.maxHeight }}</td>
    </tr>
    <tr>
        <th>{{ 'type'|babylon }}</th>
        <td>{{ entity.type }}</td>
    </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_zone') }}">
            {{ 'back.to.the.list'|babylon }}
        </a>
    </li>
    <li>
        <a href="{{ path('admin_zone_edit', { 'id': entity.id }) }}">
            {{ 'edit'|babylon }}
        </a>
    </li>
    <li>
        <form action="{{ path('admin_zone_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">{{ 'delete'|babylon }}</button>
        </form>
    </li>
</ul>
{% endblock %}