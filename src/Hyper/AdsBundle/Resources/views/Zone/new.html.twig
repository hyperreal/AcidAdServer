{% extends "::base.html.twig" %}
{% block title %}
{{ 'new.zone'|trans({}, 'HyperAdsBundle') }}
{% endblock %}
{% block body %}
<h1>Zone creation</h1>

<form action="{{ path('admin_zone_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_zone') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}