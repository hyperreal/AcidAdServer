{% extends "::base.html.twig" %}

{% block title %}
{{ 'announcement.plural.yours'|babel }}
{% endblock %}

{% block body %}
<h1>{{ 'announcement.plural.yours'|babel }}</h1>

{% if announcements|length > 0 %}
<table class="table table-striped">
    <thead>
    <tr>
        <th>{{ 'id'|babel }}</th>
        <th>{{ 'title'|babel }}</th>
        <th>{{ 'actions'|babel }}</th>
    </tr>
    </thead>
    <tbody>
    {% for announcement in announcements %}
    <tr>
        <td>{{ announcement.id }}</td>
        <td>{{ announcement.title }}</td>
        <td>
            <a href="{{ url('user_announcement_edit', {'announcement': announcement.id }) }}">Edytuj</a>
        </td>
    </tr>
    {% endfor %}
    </tbody>
</table>
{% else %}
<div class="notice message">{{ 'announcements.list.empty'|babel }}</div>
{% endif %}

<div class="additional-links">
    <a href="{{ url('user_announcement_new') }}" class="btn pull-right">{{ 'announcement.add'|babel }}</a>
</div>

{% endblock %}